import React from 'react';
import ListView from '../ListView';
interface Props {}

const Courses = () => {
    return (
        <ListView title='Courses'>
            <div className='list-item'>a</div>
        </ListView>
    );
};

export default Courses;
